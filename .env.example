PORT=3000
DATABASE_URL=postgresql://schautrack:schautrack@localhost:5432/schautrack
SESSION_SECRET=please-change-me
COOKIE_SECURE=false
SUPPORT_EMAIL=schautrack-support@schauer.to

# Postgres (docker-compose)
POSTGRES_DB=schautrack
POSTGRES_USER=schautrack
POSTGRES_PASSWORD=schautrack

# SMTP (optional, for password recovery emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com
SMTP_SECURE=false

# AI Features (optional, for photo calorie estimation)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY_ENCRYPTION_SECRET=
